# CLI Web UI ExecPlans

This shared UI root follows the shared ExecPlan contract in [`../../PLANS.md`](../../PLANS.md). Read that file in full before authoring or implementing an ExecPlan here.

## Shared-root notes

- Shared UI plans live under `plans/`.
- `ARCHITECTURE.md` is the source of truth for ownership boundaries between this root and the product-owned Flow and Workbench trees.
- Monorepo-owned workspace commands live in [`../../CONTRIBUTING.md`](../../CONTRIBUTING.md), and the shared public-skills maintainer flow lives in [`../../PUBLIC_SKILLS.md`](../../PUBLIC_SKILLS.md); do not duplicate that material in new shared-ui plans.
